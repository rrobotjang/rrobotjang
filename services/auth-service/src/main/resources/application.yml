spring:
  application:
    name: auth-service

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info

security:
  jwt:
    secret: ${JWT_SECRET:change-me-to-a-32-byte-minimum-secret-key}
    expiration-seconds: 3600
